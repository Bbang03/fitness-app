# 차곡 돌 정령 성장 명세

## 목적

완료한 운동 기록이 돌 정령의 체형에 반영된다. 상체와 하체 중 한쪽만 반복하면 해당 부위가 더 크게 보이고, 반대 부위 운동을 권해 사용자가 균형 잡힌 기록을 이어가도록 돕는다.

## 데이터 원칙

- 별도 캐릭터 저장소를 만들지 않는다.
- 기존 `WorkoutLog[]`에서 매번 성장 상태를 계산한다.
- `finished_at`이 있는 완료 운동만 반영한다.
- 무게나 총 볼륨이 아닌 완료 세트 수를 성장 단위로 사용한다.
- 같은 회차에서 같은 종목은 최대 6세트까지만 반영한다.
- 기본 운동은 `EXERCISE_DB`의 부위 분류를 사용한다.
- 사용자 운동은 이름의 운동 키워드로 보조 분류하며, 판단할 수 없는 종목은 임의 반영하지 않는다.

## 부위 매핑

| 정령 부위 | 운동 부위 |
| --- | --- |
| 상체 | 가슴, 등, 어깨, 이두, 삼두 |
| 하체 | 하체 |
| 코어 | 복근 |
| 활동 | 유산소 |

유산소 세트는 전체 성장 단계에는 반영하지만 상·하체 크기는 바꾸지 않는다.

## 성장 단계

부위 단계는 누적 세트 `0 / 1 / 8 / 20 / 40 / 72`에서 0–5단계로 상승한다.

전체 단계는 모든 분류된 누적 세트 `0 / 4 / 20 / 50 / 100 / 180`에서 0–5단계로 상승한다.

전체 단계 명칭:

1. 잠든 돌
2. 새싹 돌
3. 단단한 돌
4. 쌓이는 돌
5. 수호석
6. 차곡 수호자

## 균형 판정

- 상체와 하체 합계가 6세트 미만이면 `형성 중`이다.
- 충분한 기록이 모이면 `작은 쪽 세트 / 큰 쪽 세트` 비율을 계산한다.
- 비율이 75% 이상이면 균형 상태다.
- 75% 미만이면 적게 기록된 부위의 운동을 안내한다.
- 이 비율은 건강 점수나 의학적 평가가 아니며, 운동 기록의 분포만 나타낸다.

## 시각 규칙

- SVG 한 개를 코드로 직접 그리며 별도 이미지 다운로드가 없다.
- 상체·하체·코어가 각각 독립적으로 커진다.
- 최대 크기 차이를 제한해 캐릭터가 징그럽거나 과도하게 근육질로 보이지 않게 한다.
- 둥근 블록, 짧은 팔다리, 작은 표정을 유지한다.
- 운동 기록이 없으면 눈을 감은 상태로 표시한다.
- 스크린 리더에는 현재 상체·하체·코어 단계를 문장으로 제공한다.

## 화면

홈 상단에 다음을 한 카드로 표시한다.

- 현재 전체 단계와 단계명
- 돌 정령 시각 상태
- 상체·하체·코어의 누적 세트와 단계 진행도
- 현재 균형에 따른 한 줄 안내
- 다음 성장까지 필요한 세트 수
- 완료 운동 횟수

기존 내비게이션, 운동 저장, 식단, CV, 인증 기능은 변경하지 않는다.

## 현재 제외 범위

- 아이템과 의상
- 성장 초기화 및 되돌리기
- 서버에 별도 캐릭터 상태 저장
- 친구 비교와 순위
- 의학적 체형 평가
